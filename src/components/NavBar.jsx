function NavBar(){
    return (
        <nav>
            <a className="cursor-pointer">Home</a>
            <a className="cursor-pointer">Biblioteca</a>
            <a className="cursor-pointer">Faq</a>
            <a className="cursor-pointer">Contato</a>
            <a className="cursor-pointer">Cadastro</a>
        </nav>
    )
}

export default NavBar